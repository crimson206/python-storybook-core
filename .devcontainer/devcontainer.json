{
    "name": "Anaconda (Python 3)",
    "build": { 
        "context": "..",
        "dockerfile": "Dockerfile"
    },
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y git-flow",
    "customizations": {
        "vscode": {
        "extensions": [
            "ms-python.python",
            "ms-toolsai.jupyter",
            "eamodio.gitlens"
            ]
        }
    }
}